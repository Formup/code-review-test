class TicTacToe {
    addCross(x: number, y: number) {
        // Add cross to player1
    }

    addNill(x: number, y: number) {
        // Add nill to player2
    }

    checkForWinner(player1: {x: number, y: number}[], player2: {x: number, y: number}[]) {
        
    }
}